# sample-repo

A fixture repository used by contextpack's tests. It has nothing to do with
contextpack itself - it exists only to give the scoring and allocation
algorithms a small, realistic-looking set of files to run against.

It contains an authentication module (login and session handling), an
unrelated logging utility, and an unrelated billing module.
